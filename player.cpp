#include "player.h"
#include <string>
using namespace std;

void Player::doAction(string verb, string noun)
{
	if(verb == "GO")
		if(noun == "NORTH")
		else if(noun == "SOUTH")
		else if(noun == "EAST")
		else
			r = currentR()
}